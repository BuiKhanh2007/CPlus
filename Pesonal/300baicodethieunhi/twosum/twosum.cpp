#include <bits/stdc++.h>
using namespace std;
int main()
{
    unordered_set<int> k;
    unordered_set<int>::iterator it;
    int n,t,a,i;
    cin>>n>>t;
    for (i=0;i<n;i++)
    {
        cin>>a;
        if (k.find(k.begin(),k.end(),3)!=k.end())
        {
            break;
        }
        else cout<<"sb";    }

    return 0;

}
